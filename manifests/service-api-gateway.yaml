apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: sa-p8
  labels:
    app: api-gateway
spec:
  type: LoadBalancer
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
